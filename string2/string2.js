var string = window.prompt("Enter a string")

var numberString = window.prompt("Enter a number");

var number = Number(numberString);

var repeat = ""
for (var i = 0; i < number; i++) {
    window.alert(repeat += string + "\n")
}